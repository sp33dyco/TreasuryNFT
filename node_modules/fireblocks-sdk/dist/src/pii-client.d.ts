import PIIsdk from "@notabene/pii-sdk";
import { TransactionArguments, TravelRuleEncryptionOptions, TravelRuleOptions } from "./types";
export declare class PIIEncryption {
    private readonly config;
    toolset: PIIsdk;
    constructor(config: TravelRuleOptions);
    hybridEncode(transaction: TransactionArguments, travelRuleEncryptionOptions?: TravelRuleEncryptionOptions): Promise<TransactionArguments>;
    private travelRuleMessageHandler;
}
